package com.email.services.rabbitService;

import com.email.dto.RabbitDto;
import org.springframework.stereotype.Service;

@Service
public interface RabbitService {
//    public void receivedMessage(RabbitDto incomingMessage);
}
